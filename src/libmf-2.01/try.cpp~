#include <iostream>
#include <vector>
using namespace std;

int main()
{
	vector< vector<int> > vec(10);
	cout << vec.size() << endl;
	for(int i = 0; i < 10; i ++)
		cout << vec[i].size() << endl;
	return 0;
}


